class CheckBit {
    static boolean checkKthBit(int n, int k) {
        // code here
        int bitMask=1<<k;
        int ans=bitMask&n;
        if(ans!=0){
            return true;
        }
        
        return false;
    }
}
