class Solution {
    public int setBits(int n) {
        // code here
        int count=0;
        while(n!=0){
            count++;
            n=n&(n-1);  // rightmost bits ko zero krta jata h 
        }
        
        return count;
    }
}
