class Solution {
    public static int countBitsFlip(int a, int b) {
        // code here
        int ans=a^b;
        int count=0;
        while(ans!=0){
           
            ans=ans&(ans-1);
            count++;
        }
        return count;
    }
}
